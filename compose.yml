services:
  app:
    build:
      context: .
      target: builder
    ports:
      - "6006:6006"
    volumes:
      - .:/app
      - /app/node_modules

  prod:
    build:
      context: .
      target: app
    ports:
      - "8030:8030"

